#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(void) 
{
    char word[50];
    printf("enter shit: ");
    scanf("%s",word);
    int countLen=0;
  /*  for(int i=0;i<strlen(word);i++)
    {
        countLen++;
    }
    printf("StrLen: %ld and CountLen: %d",strlen(word),countLen);
    */
   

    return 0;
}
